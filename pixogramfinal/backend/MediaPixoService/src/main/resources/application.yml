server:
  port: 8022
spring:
  h2:
    console:
      path: /console
      enabled: true
      settings:
        web-allow-others: true
  application:
    name: media-service
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
    
    
   
